# LANCast
LANCast is an SDN-based one-to-many screen broadcasting system for offline LANs. It lets a single broadcaster stream their screen to multiple receivers without internet. Using Java and UDP multicast with an SDN controller, the system optimizes traffic with flow rules to reduce latency, avoid congestion, and ensure smooth, real-time delivery
